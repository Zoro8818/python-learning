# 学习笔记
# 查看哪些文件被修改了
git status

# 添加所有修改
git add .

# 提交到本地仓库
git commit -m "本次修改说明"

# 上传到 GitHub
git push

例如今天更新了 README，就是：

git add README.md
git commit -m "更新README文档"
git push

或者更省事一点：

git add .
git commit -m "更新README文档"
git push
本目录用于存放学习笔记。